# file-analyzer
A multi-language static repository analyzer: ingests a git repo, classifies every file across 300+ programming languages and hundreds of data/schema/binary/config/markup formats, and emits a normalized SQLite/PostgreSQL database (+ .sql dump) with installed analysis views — plus concurrent, strictly read-only Go &amp; Java readers.
